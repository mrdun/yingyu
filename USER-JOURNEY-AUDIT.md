# USER-JOURNEY-AUDIT — 商业化 MVP 用户旅程审查 (TASK-002-J-02)

> 审查方式: 只从真实用户视角走一遍完整路径, 不做全量代码审计。
> 基线: `feature/commercial-v2` @ `cbc3414`。

---

## 1. 当前用户流程 (走查结果)

| #   | 环节         | 入口                           | 用户看到什么                                     | 是否存在阻塞                                                                           |
| --- | ------------ | ------------------------------ | ------------------------------------------------ | -------------------------------------------------------------------------------------- |
| 1   | 进入网站     | `/` (未登录 → `Landing/*`)     | Banner「像玩游戏一样练英语」+ 功能/评价/FAQ/联系 | 否 (价值主张清晰)                                                                      |
| 2   | 开始使用     | 首页「开启学习」               | 直接跳 `/course-pack` 课程广场                   | 否                                                                                     |
| 3   | 登录/注册    | Logto 弹窗 → `/callback`       | 登录后回到应用                                   | 否                                                                                     |
| 4   | 浏览课程     | `/course-pack`                 | 课程卡片 (免费/会员标记)                         | **是**: 顶部没有任何「会员才能学哪些课」的说明, 用户点进会员课才被拦                   |
| 5   | 查看会员方案 | 侧边栏「会员」→ `/membership`  | 方案卡片 (价格来自 API)                          | **是**: 只有价格和天数, 没有「为什么买」「买了能得到什么」「不同方案区别」             |
| 6   | 选择方案     | `/membership`                  | 卡片列表                                         | **是**: 没有推荐方案标记; 高亮写死 `plan.id === 'yearly'`, 后台新增/改名方案后推荐失效 |
| 7   | 购买         | `/membership`                  | 支付方式选择 + 二维码/链接                       | **是**: 支付中只有一句「支付完成后会自动确认」; 失败/超时没有明确文案与重试入口        |
| 8   | 成为会员     | 支付回调                       | 仅刷新状态卡                                     | **是**: 成功后没有「去学习」的下一步引导                                               |
| 9   | 进入课程     | `/course-pack`                 | 课程列表                                         | **是**: 会员权益生效后没有任何确认反馈                                                 |
| 10  | 推广赚钱     | 侧边栏「推广中心」→ `/partner` | 链接 + 一句佣金比例                              | **是**: 不知道「怎么赚钱」「能赚多少」「佣金什么时候到手」; 状态含义无解释             |

补充发现:

- `components/Landing/PayCard.vue` 是**未被任何页面引用的死代码**, 内含硬编码美金价格 (`$19`)、旧版「免费/终身付费」方案, 以及「7 天内 100% 退款」的宣传语 —— 与当前真实退款政策 (退款保护期由后台配置) 不一致。一旦被重新引用会造成价格与承诺错误。
- 侧边栏 (WorkNav) 已有「会员」「推广中心」入口, 主要功能的**可发现性没问题**, 问题集中在页面内部的信息表达。
- 商品价格、权益、佣金比例的后端数据源都已具备 (`/plans`、`plan_entitlements`、`/partner/me`、`/partner/referrals`、`/partner/commissions`), 但前端**只用了价格**, 其余数据没有展示出来。

## 2. 影响转化的问题

### 2.1 用户是否知道这个产品解决什么问题

基本知道 (Landing 的 Banner/Features)。**未修改**, 只在课程页补一句「免费课程可直接学」的说明。

### 2.2 用户是否知道为什么购买会员 (P0)

不知道。会员页只有价格卡片, 没有价值说明与权益展示。
→ 已增加「为什么开通会员」+ **权益标签 (来自 `plan_entitlements` API)**。

### 2.3 用户是否知道不同方案的区别 (P0)

只能看到「¥18 / ¥168 / ¥199」和天数, 无法判断该买哪个。
→ 已增加: 每天成本 (由 API 价格÷时长计算)、**「最划算」推荐 (数据驱动, 不再写死 yearly)**、相对月付的节省金额与折扣 (API 价格计算)、永久方案单独区块。

### 2.4 用户是否知道购买后获得什么 (P0)

不知道 (支付成功后只刷新状态卡)。
→ 已增加支付成功态: 「🎉 支付成功, 会员已生效」+ 有效期 + **「进入课程 →」** 按钮。

### 2.5 用户是否知道 Partner 如何赚钱 (P0)

只有一句佣金比例, 没有流程和收益感知。
→ 已增加: 当前佣金比例 (API) + 计划级比例 + **4 步推广流程** + **收益计算示例 (方案价格 API × 佣金比例 API)** + 邀请记录 + 佣金汇总 + 5 种状态解释 (holding/pending/payable/paid/reversed)。

### 2.6 支付过程中的不确定感 (P1)

等待支付时缺少「正在等待/自动检测」的明确反馈; 失败与超时没有分别说明。
→ 已增加四态面板: 等待支付 (spinner + 倒计时 + 手动检测) / 成功 / 失败 (含原因与重试) / 超时关闭 (含重新购买)。

### 2.7 首次进入不知道该做什么 (P1)

→ 课程页顶部增加一行引导: 免费课程可直接学、会员课需开通会员, 并按登录/会员状态显示「登录」或「查看会员方案」。

### 2.8 微信生态下的移动端体验 (P1)

会员卡片原来固定 3 列 (手机上被压扁)、Partner 收益信息缺失; 支付按钮与二维码区域在小屏上拥挤。
→ 方案卡片改为自适应列 (`auto-fit, minmax(220px, 1fr)`), 支付/推广区块改为流式换行布局, 关键按钮保持可点区域。

## 3. 优先级

**P0 (直接影响付费转化, 本次已修)**

1. 会员页缺少价值主张与会员权益 → 从 `plan_entitlements` API 渲染。
2. 无方案对比与推荐 (且推荐写死 yearly) → 每天成本 + 数据驱动推荐 + 节省金额。
3. 支付成功后无下一步 → 成功态 + 进入课程。
4. Partner 页无法回答「怎么赚钱 / 能赚多少 / 什么时候到手」→ 流程 + 收益示例 + 状态说明。

**P1 (影响体验与流失, 本次已修)**

5. 支付失败/超时无明确提示与重试入口 → 四态支付面板。
6. 首次进入缺少方向 → 课程页引导条。
7. 移动端卡片固定列数 → 自适应网格。
8. 死代码 `PayCard.vue` 含硬编码价格与不符政策的退款承诺 → 已删除。

**P2 (记录, 本次不做)**

9. 缺少真实二维码图片渲染 (需引入 QR 依赖; 当前展示支付码 + 复制)。
10. 推广素材中心 / 分享海报 / 一键分享到微信。
11. 支付结果页仍依赖轮询, 未接 WebSocket 实时推送。
12. 会员到期提醒 (站内通知/邮件)。
13. 课程页缺少按会员权益的课程推荐/差异化排序。

## 4. 本次实际修改范围

前端 (无后端业务逻辑改动):

- `pages/membership.vue` — 价值主张、权益 (API)、方案对比/推荐、永久方案区块、支付四态、移动端布局。
- `pages/partner.vue` — 佣金比例 (API)、推广流程、收益示例 (API 价格 × API 比例)、邀请记录、佣金汇总、状态说明。
- `pages/course-pack/index.vue` — 首次进入引导条 (含会员状态)。
- `utils/membership-ui.ts` (+ 测试) — 展示计算纯函数 (价格格式化、每天成本、推荐、节省、佣金示例、支付阶段映射)。
- `api/membership.ts` — 计划 id 不再枚举 (后台新增方案不破类型); 增加权益字段类型。
- `api/partner.ts` — 新增邀请记录 / 佣金汇总调用。
- 删除 `components/Landing/PayCard.vue` (死代码 + 硬编码价格 + 不符政策的退款宣传)。

后端 (只读展示补全, 不动支付/权限/佣金逻辑):

- `GET /plans` 响应增加 `entitlements` (来自 `plan_entitlements`), 让前端权益文案不硬编码。

不做: 数据库结构、支付状态机、权限模型、佣金模型、任何新业务功能。
