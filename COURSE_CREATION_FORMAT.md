# 课程交换格式规范 v1（含语法标注）

> 状态：**草案 v1.1，待拍板**。定稿后发布给第三方工具制作方。
> 用途：定义「一份课程数据」的标准形态，让三条路径共用同一份格式：
> ① AI 管道生成课程 ② 第三方工具制作后导入 ③ 前台展示语法标注。
>
> 本版的关键设计已用**真实语料 + 真实模型调用**验证过（见 §11「验证记录」）。

---

## 0. 一句话概括

一份 JSON 描述「一个课程包 → 若干课 → 若干句子」，**每个句子可带一份语法标注**。
标注分两层：**逐词标词性**（词下方的小标签）+ **短语标成分**（词上方的气泡）。
所有标注都带**字符位置**，前台才能把标签画到对应的词上。

---

## 1. 四条硬性约束（先看这四条，其余都是细节）

### 1.1 字符位置是必需的，且必须能对上

每条标注给出 `start` / `end`（英文原句里的**字符下标**，左闭右开），必须满足：

```
english.slice(start, end) === text
```

对不上 → **整个文件拒绝导入**。前台要用它画连线，位置错了标注就会指错词，
把「宾语」标到「谓语」头上——**教错语法比不教更糟**。

> **给实现方的建议（重要）**：**不要让大模型去数字符位置**（LLM 数不准上百字符的长句）。
> 让模型只输出**短语原文**，偏移由程序按空白走位算出来。这样 E4/E6/E7 在原理上不可能违反。
> 我们的实测就是这么做的：22 条句子、0 次偏移错误。

### 1.2 词性逐词标，成分按短语标 —— 两层，不要混

```
        ┌主语┐ ┌────谓语────┐ ┌───宾语───┐ ┌状语┐   ← 短语级 (phrases)
        I      don't like    to do it     now
       代词    助动词 动词     不定式 动词 代词  副词    ← 逐词级 (words)
```

⚠️ **反面教材**（某产品的实际标注，教学上站不住）：把 `learning` 和 `English` **各自**标成「宾语」。
`learning English` 整体才是一个宾语（动名词短语）。
**逐词都标成分会让学习者以为每个词都是一个成分。**

⚠️ **反过来也错**：碎片（如 `to do it`）只给一个「不定式」标签、丢掉逐词词性。
碎片恰恰**最需要**逐词词性（学习者正在认单词）。所以 `words` 和 `phrases` **都要给**。

### 1.3 语料不含句末标点 → 是否「完整句」必须由标注方给出

实测我方语料 **8,865 条英文全部没有句末标点**（`.` `?` `!` `,` 出现 **0** 次），
所以**无法用标点判断**一个单元是不是完整句。而语料是「连词成句」教学法，
48% 的单元是 `to see` / `I like` 这种**碎片**：

| 词数                  | 条数（全库） | 能否标成分    |
| --------------------- | ------------ | ------------- |
| 1–2 词（如 `to see`） | 4,260        | ❌ 只能标词性 |
| 3–4 词                | 1,852        | 🟡 多数不能   |
| 5–8 词                | 2,121        | ✅            |
| 9+ 词                 | 632          | ✅            |

**所以必须显式给出 `isSentence`**，不能靠启发式猜
（实测「以主语代词开头」这类启发式覆盖不全：有 682 条唯一句 5 词以上但不是代词开头，
其中既有碎片也有完整句）。

### 1.4 缺标注是合法状态，编造不是

`grammar` 整体缺失 → 前台不显示标注面板，功能照常可用。
**不允许**为了填满字段而编造标注。没把握就 `confidence` 给低分或不给 `grammar`。

---

## 2. 版本与兼容

| 项                       | 规则                                             |
| ------------------------ | ------------------------------------------------ |
| `formatVersion`          | 整数，本规范为 `1`。**必填**                     |
| 高版本文件被低版本系统读 | **拒绝**并提示升级（不做猜测性兼容）             |
| 未知字段                 | **忽略**（不报错），以便将来加字段不破坏老解析器 |

---

## 3. 顶层结构

```jsonc
{
  "formatVersion": 1,
  "coursePack": {
    /* §4.1 */
  },
  "courses": [
    /* §4.2，至少 1 个 */
  ],
}
```

---

## 4. 字段定义

### 4.1 `coursePack`（课程包）

| 字段          | 类型                       | 必填 | 说明                                   |
| ------------- | -------------------------- | ---- | -------------------------------------- |
| `title`       | string                     | ✅   | 课程包名，1–60 字                      |
| `description` | string                     |      | 简介，≤200 字                          |
| `cover`       | string                     |      | 封面图 URL 或 `/public` 下的相对路径   |
| `isFree`      | boolean                    |      | 是否免费；缺省按**付费**处理（更安全） |
| `accessLevel` | `"free"` \| `"membership"` |      | 与 `isFree` 二选一；同时给出时必须一致 |
| `order`       | integer                    |      | 展示顺序，缺省 0                       |

### 4.2 `courses[]`（课）

| 字段          | 类型    | 必填 | 说明                |
| ------------- | ------- | ---- | ------------------- |
| `title`       | string  | ✅   | 如「第一课」        |
| `order`       | integer | ✅   | 课序号，从 1 开始   |
| `description` | string  |      |                     |
| `video`       | string  |      | 关联视频 URL        |
| `statements`  | array   | ✅   | §4.3，**至少 1 条** |

### 4.3 `statements[]`（句子 / 学习单元）

| 字段                | 类型                               | 必填 | 说明                                     |
| ------------------- | ---------------------------------- | ---- | ---------------------------------------- |
| `order`             | integer                            | ✅   | 课内顺序，从 1 开始                      |
| `chinese`           | string                             | ✅   | 中文                                     |
| `english`           | string                             | ✅   | 英文原文。**所有字符位置以本字段为基准** |
| `soundmark`         | string                             |      | 音标；没把握留空串                       |
| `sourceType`        | `"text"` \| `"audio"` \| `"video"` |      | 缺省 `text`                              |
| `audioUrl`          | string                             |      |                                          |
| `startMs` / `endMs` | integer                            |      | 音视频时间轴                             |
| `grammar`           | object                             |      | §4.4。**可选**：没有就不显示标注面板     |

### 4.4 `grammar`（语法标注）—— 本规范新增的核心

| 字段           | 类型                                                 | 必填 | 说明                                                                          |
| -------------- | ---------------------------------------------------- | ---- | ----------------------------------------------------------------------------- |
| `isSentence`   | boolean                                              | ✅   | 是否**完整句**（§1.3）                                                        |
| `unitType`     | `"sentence"` \| `"clause"` \| `"phrase"` \| `"word"` |      | 单元类型                                                                      |
| `pattern`      | string                                               | ✅   | 句型符号式，符号**必须**取自 §5.4 闭集，用 `+` 连接。**仅**完整句必填         |
| `structure`    | string                                               |      | 句子结构的中文显示串。**必须由 `pattern` 按 §5.5 映射表得出**，不允许自由发挥 |
| `clauseType`   | enum                                                 |      | `简单句` \| `并列句` \| `复合句`（§5.7）。**仅**完整句                        |
| `sentenceType` | enum                                                 |      | §5.2。**仅**完整句                                                            |
| `tense`        | enum                                                 |      | §5.3。**仅**完整句                                                            |
| `keyPoints`    | string[]                                             |      | 语法要点，每条 ≤40 字，**最多 3 条**；没把握就少给，不要凑数                  |
| `confidence`   | number                                               |      | 0–1，标注方自评置信度                                                         |
| `words`        | array                                                | ✅   | §4.5 逐词词性，**每个词恰一条**                                               |
| `phrases`      | array                                                | ✅   | §4.6 短语成分，**覆盖所有词**                                                 |

> ⚠️ **为什么 `structure` 不能自由写**（完整一课实测教训）：只给一句「如 `主谓宾 + 时间状语`」的示例、
> 不做闭集约束，模型会在同一份课程里用 `主系表`、`主 + 系 + 表`、`主系表 + 状语`、
> `主系表 + 不定式短语(真实主语)` **四种写法表达同一件事**。实测「第一课」的 100 条完整句里
> 出现了 **20 种以上**写法 —— 学习者看到的就是「同类句子标签不一致」。
> 所以：**`pattern` 是唯一真相（闭集符号），`structure` 只是它的中文渲染。**

### 4.5 `grammar.words[]`（逐词词性）

| 字段            | 类型    | 必填 | 说明                       |
| --------------- | ------- | ---- | -------------------------- |
| `text`          | string  | ✅   | **恰好一个词**（不含空格） |
| `start` / `end` | integer | ✅   | 字符位置                   |
| `pos`           | enum    | ✅   | 词性（§5.1）               |

**规则**（可 100% 确定性校验，这是最容易做对的一层）：

1. `words` 里的 `text` 按顺序拼起来（用单个空格连接）**必须与 `english` 完全一致**
   （大小写一致、词数一致、不加标点）。
2. 换句话说：`[w.text for w in words] === english.split(" ")`。

### 4.6 `grammar.phrases[]`（短语成分）

| 字段            | 类型         | 必填 | 说明                                      |
| --------------- | ------------ | ---- | ----------------------------------------- |
| `text`          | string       | ✅   | 被标注片段（**可含多个词**）              |
| `start` / `end` | integer      | ✅   | 字符位置                                  |
| `role`          | enum \| null |      | 句子成分（§5.5）。非完整句**必须** `null` |
| `roleType`      | enum \| null |      | 该成分的短语类型（§5.6）                  |
| `note`          | string       |      | 该处特别说明，≤30 字；没把握不给          |

**规则**：

1. `phrases` 里的 `text` 按顺序拼接也必须**与 `english` 完全一致**
   （即：`phrases` 是**同一串词序列的另一种切分**，允许把相邻词合成一段，不得跳词/重排/拆分/重复）。
2. 按 `start` 升序，**不得重叠**。
3. `isSentence=false` 时：`role` 与 `roleType` **必须都是 `null`**。
   此时可以把整句作为**一个** `phrases` 条目（只为满足结构要求），也可以逐词列。
4. `isSentence=true` 时：`structure` 必填；成分按**短语**切分
   （`don't like` 整体=谓语，`to do it` 整体=宾语），**不要**逐词都标成分（见 §1.2）。

---

## 5. 取值表（**闭集**，必须从下表取值）

> 取值不一致会让同类句子在不同句子里显示不同标签——学习者越看越糊涂。
> 出现表外的值 → **拒绝导入**。

### 5.1 `pos`（词性）

`名词` `代词` `动词` `助动词` `情态动词` `形容词` `副词` `介词` `连词` `冠词` `数词` `动名词` `不定式` `分词` `感叹词`

### 5.2 `sentenceType`（句型）

`陈述句` `疑问句` `祈使句` `感叹句` `there be 句型`

### 5.3 `tense`（时态）

`一般现在时` `一般过去时` `一般将来时` `现在进行时` `过去进行时` `将来进行时`
`现在完成时` `过去完成时` `将来完成时` `现在完成进行时` `过去将来时`

### 5.4 `pattern`（结构式）

固定符号，用 `+` 连接。**这就是唯一真相**，`structure` 由它映射而来（§5.5）：

| 符号       | 含义     | 符号     | 含义       |
| ---------- | -------- | -------- | ---------- |
| `S`        | 主语     | `OC`     | 宾语补足语 |
| `V`        | 谓语     | `C`      | 补语       |
| `O`        | 宾语     | `Attrib` | 定语       |
| `P`        | 表语     | `Adv`    | 状语       |
| `IO`       | 间接宾语 | `conj`   | 连接词     |
| `DO`       | 直接宾语 | `Clause` | 从句       |
| `There be` | There be |          |            |

例：`S + V + O`、`S + V + O + Adv`、`S + V + IO + DO`、`There be + S + Adv`、
并列句 `S + V + O + conj + S + V + O`、复合句 `S + V + Clause`。

⚠️ **符号必须逐个取自上表，不得自造**。实测被模型写过的不合规写法（都会触发 E13）：
`S + V + O + 时间状语`（应写 `S + V + O + Adv`）、`It + is + adj + to do sth`、
`S + V + Predicative`（应写 `S + V + P`）、`S + have to + V + O`。

状语的**细分**（时间 / 地点 / 原因…）与从句内容放进 `phrases[].note`，**不要**污染 `pattern`。

### 5.5 `structure`（句子结构中文串）—— 必须由 `pattern` 映射，禁止自由书写

按下表逐符号翻译，用 `+` 连接：

| `pattern` 符号 | 中文     | `pattern` 符号 | 中文       |
| -------------- | -------- | -------------- | ---------- |
| `S`            | 主语     | `OC`           | 宾语补足语 |
| `V`            | 谓语     | `C`            | 补语       |
| `O`            | 宾语     | `Attrib`       | 定语       |
| `P`            | 表语     | `Adv`          | 状语       |
| `IO`           | 间接宾语 | `conj`         | 连接词     |
| `DO`           | 直接宾语 | `Clause`       | 从句       |
| `There be`     | There be |                |            |

**映射示例**：

| `pattern`                      | 必须得到的 `structure`                             |
| ------------------------------ | -------------------------------------------------- |
| `S + V + O`                    | `主语 + 谓语 + 宾语`                               |
| `S + V + O + Adv`              | `主语 + 谓语 + 宾语 + 状语`                        |
| `S + V + P`                    | `主语 + 谓语 + 表语`                               |
| `S + V + IO + DO`              | `主语 + 谓语 + 间接宾语 + 直接宾语`                |
| `S + V + O + conj + S + V + O` | `主语 + 谓语 + 宾语 + 连接词 + 主语 + 谓语 + 宾语` |
| `S + V + Clause`               | `主语 + 谓语 + 从句`                               |
| `There be + S + Adv`           | `There be + 主语 + 状语`                           |

校验：`structure` 与映射结果不一致 → **拒绝导入（E13）**。

> 💡 **实现建议（强烈）**：**不要让模型写 `structure`** —— 只让它输出闭集 `pattern`，
> 由**程序**按本表算出 `structure`。这与「不让模型数字符位置」同一条原则：
> 凡是程序能确定性做对的，就别交给模型。
> 实测：让模型自己写 `structure` 时，100 条完整句全部与 `pattern` 对不上（写法五花八门）；
> 改成程序映射后，E13 变成**构造性成立**，不可能失败。

### 5.6 `role`（句子成分）

`主语` `谓语` `宾语` `表语` `定语` `状语` `补语` `宾语补足语` `同位语` `连接词` `插入语` `引导词`

### 5.7 `roleType`（短语类型）

`名词短语` `动词短语` `形容词短语` `副词短语` `介词短语` `动名词短语` `不定式短语` `分词短语` `从句` `单词`

### 5.8 `clauseType`（句子种类）

`简单句` `并列句` `复合句`

---

## 6. 完整示例（真实语料，已用校验器验过：0 error / 0 warning）

原句（本库「第一课」第 19 条）：`I don't like to do it now`

```json
{
  "formatVersion": 1,
  "coursePack": {
    "title": "零基础学英语",
    "description": "从零开始，用连词成句的方式掌握最常用的英语表达",
    "isFree": true,
    "accessLevel": "free",
    "order": 0
  },
  "courses": [
    {
      "title": "第一课",
      "order": 1,
      "statements": [
        {
          "order": 19,
          "chinese": "我不喜欢现在做这件事情",
          "english": "I don't like to do it now",
          "soundmark": "aɪ doʊnt laɪk tu du ɪt naʊ",
          "grammar": {
            "isSentence": true,
            "unitType": "sentence",
            "pattern": "S + V + O + Adv",
            "structure": "主语 + 谓语 + 宾语 + 状语",
            "clauseType": "简单句",
            "sentenceType": "陈述句",
            "tense": "一般现在时",
            "keyPoints": [
              "don't = do not，第一人称用 do 不用 does",
              "like to do sth：喜欢做某事",
              "now 放句末作时间状语"
            ],
            "confidence": 0.95,
            "words": [
              { "text": "I", "start": 0, "end": 1, "pos": "代词" },
              { "text": "don't", "start": 2, "end": 7, "pos": "助动词" },
              { "text": "like", "start": 8, "end": 12, "pos": "动词" },
              { "text": "to", "start": 13, "end": 15, "pos": "不定式" },
              { "text": "do", "start": 16, "end": 18, "pos": "动词" },
              { "text": "it", "start": 19, "end": 21, "pos": "代词" },
              { "text": "now", "start": 22, "end": 25, "pos": "副词" }
            ],
            "phrases": [
              { "text": "I", "start": 0, "end": 1, "role": "主语", "roleType": "单词" },
              {
                "text": "don't like",
                "start": 2,
                "end": 12,
                "role": "谓语",
                "roleType": "动词短语"
              },
              {
                "text": "to do it",
                "start": 13,
                "end": 21,
                "role": "宾语",
                "roleType": "不定式短语"
              },
              {
                "text": "now",
                "start": 22,
                "end": 25,
                "role": "状语",
                "roleType": "单词",
                "note": "时间状语"
              }
            ]
          }
        },
        {
          "order": 12,
          "chinese": "做这件事情",
          "english": "to do it",
          "soundmark": "",
          "grammar": {
            "isSentence": false,
            "unitType": "phrase",
            "keyPoints": ["不定式短语，常跟在 want / like / need 之后"],
            "confidence": 0.9,
            "words": [
              { "text": "to", "start": 0, "end": 2, "pos": "不定式" },
              { "text": "do", "start": 3, "end": 5, "pos": "动词" },
              { "text": "it", "start": 6, "end": 8, "pos": "代词" }
            ],
            "phrases": [
              { "text": "to do it", "start": 0, "end": 8, "role": null, "roleType": null }
            ]
          }
        }
      ]
    }
  ]
}
```

**注意两条的差别**：完整句给了成分/结构/时态/句型；碎片只给逐词词性 + 一个无成分的短语占位，
`role`/`roleType` 是 `null`，且**没有** `structure`/`tense`/`sentenceType`。

---

## 7. 校验规则清单

实现方（含第三方工具）**应在导出前自检**；我方导入时**会再校验一次**。

### 拒绝导入（Error）

| 编号 | 规则                                                                         |
| ---- | ---------------------------------------------------------------------------- |
| E1   | `formatVersion` 不为 1                                                       |
| E2   | `courses` 为空，或含 `statements` 为空的课                                   |
| E3   | `chinese` 或 `english` 为空串（`soundmark` 可为空）                          |
| E4   | `english.slice(start, end) !== text`（**任何一条不符即拒绝**）               |
| E5   | `start < 0` 或 `end > english.length` 或 `start >= end`                      |
| E6   | 同一数组内片段重叠                                                           |
| E7   | `words` 的 `text` 序列 ≠ `english.split(" ")`（**确定性校验**）              |
| E8   | `phrases` 的 `text` 序列（拼接后）≠ `english`                                |
| E9   | 枚举值不在 §5 取值表内                                                       |
| E10  | `isSentence=true` 但 `pattern` 缺失                                          |
| E11  | `isSentence=false` 但某条 `phrases` 的 `role` 非 `null`                      |
| E12  | 同一课内 `order` 重复                                                        |
| E13  | `structure` 与 `pattern` 按 §5.5 映射的结果不一致（或 `pattern` 含自造符号） |

### 接受但告警（Warning，导入后可在后台修正）

| 编号 | 规则                                                                                                                                                 |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| W1   | `isSentence=true` 且 `phrases` 条数 == 词数且词数 > 1（疑似逐词标成分，见 §1.2。**注意**：`It is important` 这类三词三成分的短句会误报，仅为软信号） |
| W2   | `keyPoints` 超过 3 条或单条 > 40 字                                                                                                                  |
| W3   | 缺 `confidence`                                                                                                                                      |
| W4   | 同一 `english` 在文件内出现多次但标注不同（不一致）                                                                                                  |
| W5   | `confidence < 0.6`（低置信度会被前台降级显示，可能白标一场）                                                                                         |

---

## 8. 前台降级规则（约束数据形态，也约束前端）

| 情况               | 前台表现                                                   |
| ------------------ | ---------------------------------------------------------- |
| 无 `grammar`       | 完全不显示标注                                             |
| `isSentence=false` | 只显示**逐词词性**；不显示成分气泡、不显示结构/时态/句型卡 |
| `confidence < 0.6` | 只显示词性，不显示成分与结构卡                             |
| 数据不合规（E 类） | 该句整块不显示（**不能只显示一半**）                       |

---

## 9. 成本与规模（实测自本库 + 真实模型调用）

| 指标                        | 实测值                                                                                         |
| --------------------------- | ---------------------------------------------------------------------------------------------- |
| 全库句子                    | 8,865 条 / 55 课 / 1 课程包                                                                    |
| **去重后唯一句**            | **4,753 条**（可省 46% 的调用——**标注按 `english` 原文缓存**）                                 |
| 其中 5 词以上（需成分结构） | 2,226 条                                                                                       |
| 其中 1–2 词碎片             | 1,349 条                                                                                       |
| 最长句                      | 119 字符 / 20 词                                                                               |
| **单条标注实测开销**        | 约 **106 输入 / 237 输出 token**（`deepseek-v4-flash`，每批 10 条；完整一课 218 条的实测均值） |
| **全库一次性标注估算**      | 约 **50 万输入 + 113 万输出 token** ≈ **0.4 美元 / 约 3 元人民币**                             |

**缓存键 = `english` 原文**：同一句在不同课里重复出现时直接复用，不必重算。

---

## 10. 排版约束（实测，改渲染前必读）

练习页必须把**一个句子**在一屏内讲清。实测方式：**用真实效果页在真实浏览器里量**
（`scripts/grammar/measure-corpus.js`，驱动 `grammar-effect-page.html` 逐句读几何）。

| 指标（第一课 100 条完整句 · 当前版式） | 实测值                |
| -------------------------------------- | --------------------- |
| 最大单行需宽                           | **651px**（9 词那句） |
| 面板可用宽 680px 的「不折行」比例      | 95.0%                 |
| 面板可用宽 **820px 及以上**            | **100%**              |

> ⚠️ **版式一改，这里的数字就失效，必须重测。** 本轮把「胶囊里装成分词 + 成分名一行」
> 落地后，同一批句子的最大单行需宽从 600px 变到 **651px**（组间距 16→46px）；
> 早先那套按旧几何做的合成测量脚本已删除（结论已被本节取代）。

### 10.1 长句靠**从句断行**解决，不靠加宽面板、也不靠限制句子长度

规则：**遇到连接词（`so` / `and` / `but` …）另起一行**，连接词作为新行的第一个成分。
实现见 `render-effect-page.py`（`toRows()`）与 `render-preview.py` 的 `rows_of()`。

为什么：本库的「长句」几乎全是 `It is … for me so I have to …` 这一族**并列句**
（两个简单句用 `so` 连起来）。第一课 100 条里，94 条 ≤9 词，其余 **6 条是 17–21 词**，
单行分别要 1198 / 1302 / 1335 / 1453 / 1466 / 1499px（**旧版式**下的量值）。
**在 `so` 处断开后每半句只要 ~530px**，面板宽 ≥820px 就 100% 装得下。

对比另外两个方案（都实测过）：

| 方案                   | 收益                                                                                  | 代价                     | 结论           |
| ---------------------- | ------------------------------------------------------------------------------------- | ------------------------ | -------------- |
| 练习区改全宽（1400px） | 第一课 **0**（本就 100%）；一行横跨整屏，扫视距离过远                                 | 破坏居中版式             | **不做**       |
| AI 生成时限字数        | 只对新内容生效，**救不了存量句**；封顶 ≤9 词会淘汰 **7.1%** 内容、≤6 词淘汰 **25.8%** | 内容损失不可逆           | **不做硬限制** |
| **从句断行（采用）**   | 第一课 100% 不折行（≥820px）；最大需求砍半                                            | 无（不损内容、不改版式） | **采用**       |

### 10.2 若仍要给 AI 生成加**软**约束（可选的第二道保险）

以现行面板宽（可用约 960px）为准：**软上限 50 字符 / 9 词** —— 保住约 94.8% 的内容，
且这批内容在现有面板宽下几乎 100% 不折行。
**只作为生成时的偏好写进提示词，不要设成校验错误**（设成错误会否决大量合法长句）。

---

## 11. 版本变更记录

| 版本 | 日期       | 变更                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ---- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1    | 2026-09-15 | 首版：三级结构 + `grammar` 标注块 + 字符位置约束 + 闭集取值表                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 1.1  | 2026-09-15 | **探针实测后修订**：① 拆成 `words`（逐词词性）+ `phrases`（短语成分）两个数组——原设计把词性塞进短语条目，碎片只落一个标签、丢掉逐词词性；② E7 改为**确定性**校验（`words.text` 必须等于切词结果）；③ 补「不要让模型数字符位置」的实现要点；④ 补实测成本                                                                                                                                                                                                                              |
| 1.2  | 2026-09-15 | **完整一课（218 条）实测后修订**：`pattern` 升为完整句**必填**且符号闭集；新增 §5.5 映射表把 `structure` 限定为 `pattern` 的中文渲染（实测 100 条完整句出现 20+ 种自造写法）；新增 `clauseType`；新增校验 E13；W1 补注误报场景                                                                                                                                                                                                                                                       |
| 1.3  | 2026-09-15 | **完整规则集复核后修订**：明确 **`structure` 由程序按 §5.5 映射得出、模型只输出 `pattern`**（实测把 structure 交给模型写，100/100 条 E13 不合格；程序映射后 0 不合格）。`pattern` 闭集补 `conj`（连接词）与 `Clause`（从句）以覆盖并列句/复合句。补流程教训：**改规范必须同步所有校验器并重跑产物**                                                                                                                                                                                  |
| 1.4  | 2026-09-15 | **新增 §10 排版约束**（真实浏览器实测）：长句用**从句断行**（连接词另起一行），第一课最大需求 1064→600px、100% 不换行；实测否决「练习区改全宽」（第一课收益为 0）与「AI 生成限字数」（救不了存量句、≤9 词淘汰 7.1% 内容）两个方案；给出可选的**软上限 50 字符 / 9 词**                                                                                                                                                                                                               |
| 1.5  | 2026-09-15 | **明确气泡的视觉语言**：一个**成分**一个**实心饱和色胶囊**（白字，对比度 ≥4.5:1 已算过），单词**裸露不装框**，成分下方**一条横条横跨该成分全部词**。上一版给了单词独立小卡片 + 每词一条短横条 → 看起来是「一个单词一个气泡」，被用户否掉。同时修掉渲染器「按 `find()` 排序短语导致重复输出词」的缺陷（会污染宽度测量）                                                                                                                                                               |
| 1.6  | 2026-09-15 | **气泡视觉语言再修订（用户逐条拍板）**：胶囊里装的**就是该成分的英文词**（同一成分合成一个胶囊），成分名放在**胶囊上方**、与胶囊居中且**同色**，每个单词一条下划线 + 中文词性、三者**同一条中心线且同色**；下划线配色提供 **A 按词性 / B 跟随成分** 两套；结构化信息收成**四个等宽卡片**（句子结构 / 时态 / 句型 / 语法要点）。第 1.5 版的「胶囊写成分名、词在外面 + 一条跨段横条」同样被否。§10 的体积测量数字按新版式**重测**（最大单行需宽 600→**651px**，≥820px 时 100% 不折行） |

---

## 11. 验证记录（本规范怎么被验过的）

| 验证项                       | 方法                                                         | 结果                                                                                                              |
| ---------------------------- | ------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------- |
| 字符位置可行性               | 真实语料按空白切词 + 短语合并，逐条校验 `slice`              | 全部对得上（含 `don't`、`Xiaoyueliang's` 的撇号）                                                                 |
| 语料形态                     | 全库 8,865 条词数分布 + 标点扫描                             | 无句末标点；48% 是 1–2 词碎片 → 推出 `isSentence` 必需                                                            |
| 示例自身合规                 | 用校验器跑文档里的示例                                       | 0 error / 0 warning                                                                                               |
| **模型能否按本格式产出**     | **对「第一课」真实句子调 `deepseek-v4-flash` 两次（22 条）** | **两次均 22/22 合规、0 偏移错误；句子/碎片判定全对**                                                              |
| 双数组设计                   | 第二次探针专门验证 `words` + `phrases`                       | 22/22 合规；碎片保住逐词词性，成分仍为短语级（未犯 §1.2 的反面错误）                                              |
| **完整一课跑通**             | **「第一课」218 条全量标注（22 次调用）**                    | **218/218 返回**；完整句 100 / 碎片 118；置信度均值 0.931、最低 0.7                                               |
| **完整规则集复核（E1–E13）** | 对上述产物跑**完整**规则集                                   | 首轮：**E13 不合格 100/100 条**（见下）；修正后：**0 不合格**，pattern 取值收敛到 **9 种**、clauseType 100 条齐全 |

**完整一课暴露、而 22 条探针没暴露的问题**（这就是为什么要跑满一课）：

1. **`structure` 自由书写 → 同一结构多种写法**。100 条完整句里出现 20+ 种字符串，
   同一件事写成 `主系表` / `主 + 系 + 表` / `主系表 + 状语` / `主系表 + 不定式短语(真实主语)`。
   → 修订：`pattern` 必填且闭集，`structure` 由 §5.5 映射表得出，新增 E13 强制校验。
   （小样本探针只出现 2 种写法，看不出问题。）
2. **`pattern` 也被自造符号污染**。实测写成 `S + V + O + 时间状语`、`It + is + adj + to do sth`、
   `S + V + Predicative`、`S + have to + V + O` —— 都不是闭集符号。
3. **`structure` 交给模型写本身就是错的设计**。把 §5.5 落成「模型写」时，
   即使给了映射表，100 条也全部对不上。
   → **改成程序映射后 E13 构造性成立**（与「不让模型数字符位置」同一条原则）。
   修正后的对照：

   | 指标             | 模型自己写 structure | 程序按 pattern 映射 |
   | ---------------- | -------------------- | ------------------- |
   | E13 不合格       | **100 / 100 条**     | **0 条**            |
   | pattern 取值种类 | 20+ 种（含自造符号） | **9 种**（全合法）  |
   | clauseType       | 100 条全缺           | 100 条齐全          |

4. **W1 存在误报**：`It is important` 三词三成分是**正确**标注，却触发「疑似逐词标成分」。
   → 保留为软信号并补注。

> ⚠️ **教训（写进流程）**：**校验规则集本身也要版本化**。本轮出现过一次
> 「跑课脚本用的是旧规则集、新加的 E13 只在示例校验器里」——于是脚本报「0 不合格」，
> 而按完整规则集复核是 100 条不合格。**改规范后必须同步所有校验器，并在产物上重跑一遍。**

**探针中发现并修掉的问题**（记录下来，避免重犯）：

1. **一次给太多条会被 `max_tokens` 截断** → JSON 不完整。分批 5 条 + `response_format: json_object` 解决。
2. **原设计（词性塞进短语条目）在碎片上失效** → 改为双数组。
3. **原 E7「覆盖整句无空隙」过严** → 词与词之间本来就有空格，会把所有正常标注判错。
   改成「未覆盖的位置只允许是空白」。

---

## 附录 A：给第三方工具制作方的实现要点

1. **先切词、算偏移，再标语法**。切词用「按空白切分」，得到每个词的 `[start, end)`。
   - ⚠️ 缩写（`don't`）、所有格（`Xiaoyueliang's`）里的撇号**不能当分隔符**——实测本库有 554 条含撇号。
   - ⚠️ 本库语料**没有双空格、没有非 ASCII 字符**，但仍应处理（替换为单空格）。
2. **先逐词标 `pos`**（每个词必有词性），**再把相邻词合并成 `phrases` 的成分**。
3. `isSentence=false` 的单元：`role`/`roleType` 给 `null`，**不要**给 `structure`/`tense`/`sentenceType`。
4. 没把握的句子：**降低 `confidence` 或整个不给 `grammar`**，不要硬标。
5. 导出前跑一遍 §7 的 E1–E12 自检。
6. 体量参考：本库单包 55 课 / 8,865 句。建议单文件 ≤ **50MB**，更大就拆多个文件分批导入。

## 附录 B：与现有系统的关系

| 现有字段/能力                                        | 关系                                                                    |
| ---------------------------------------------------- | ----------------------------------------------------------------------- |
| `statements` 表（chinese/english/soundmark/order/…） | 与 `statements[]` 一一对应；`grammar` 是**新增**存储                    |
| `course_packs.source`                                | 已预留 `import` 取值（此前**从未使用**）——第三方导入的包标记为 `import` |
| AI 建课管道（`/ai-content/course-pack`）             | 将改为**直接产出本格式**，与导入共用同一套校验                          |
| 现有后台建课页                                       | 导入后进入 `draft`，走既有「提交审核 → 发布」流程，**不绕过审核**       |
| `statements` 表当前 0 个语法字段                     | 本格式落地需要**加列 + 跑数据库迁移**（首个真正改表结构的改动）         |
